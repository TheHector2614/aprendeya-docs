version: "3.9"

services:
  api:
    build: .
    image: aprendeya-agent:latest
    ports:
      - "8000:8000"
    environment:
      - GROQ_API_KEY=${GROQ_API_KEY}
    volumes:
      - ./index:/app/index
    restart: unless-stopped
