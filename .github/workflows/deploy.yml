name: Deploy AprendeYa Agent to OCI

on:
  push:
    branches: [main]
    paths:
      - "scripts/**"
      - "Dockerfile"
      - "docs-management/**"
      - "raw/**"
      - ".github/workflows/deploy.yml"
  workflow_dispatch:

env:
  OCI_REGION: us-ashburn-1
  OCI_COMPARTMENT_OCID: ${{ vars.OCI_COMPARTMENT_OCID }}
  OCI_OBJECT_STORAGE_NS: ${{ vars.OCI_OBJECT_STORAGE_NS }}
  DOCKER_IMAGE_NAME: aprendeya-agent
  CONTAINER_INSTANCE_ID: ${{ vars.OCI_CONTAINER_INSTANCE_ID }}

jobs:
  build-and-push:
    name: Build & Push Docker Image
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up OCI CLI
        uses: oracle-actions/setup-cli@v1
        with:
          tenancy: ${{ secrets.OCI_TENANCY_OCID }}
          user: ${{ secrets.OCI_USER_OCID }}
          fingerprint: ${{ secrets.OCI_FINGERPRINT }}
          private-key: ${{ secrets.OCI_PRIVATE_KEY }}
          region: ${{ env.OCI_REGION }}

      - name: Log in to OCIR
        run: |
          echo "${{ secrets.OCI_AUTH_TOKEN }}" | docker login \
            ${{ env.OCI_REGION }}.ocir.io \
            -u ${{ secrets.OCI_USER_NAME }} \
            --password-stdin

      - name: Build Docker image
        run: |
          docker build \
            --cache-from ${{ env.OCI_REGION }}.ocir.io/${{ env.OCI_OBJECT_STORAGE_NS }}/${{ env.DOCKER_IMAGE_NAME }}:latest \
            -t ${{ env.DOCKER_IMAGE_NAME }}:latest \
            -t ${{ env.OCI_REGION }}.ocir.io/${{ env.OCI_OBJECT_STORAGE_NS }}/${{ env.DOCKER_IMAGE_NAME }}:${{ github.sha }} \
            -t ${{ env.OCI_REGION }}.ocir.io/${{ env.OCI_OBJECT_STORAGE_NS }}/${{ env.DOCKER_IMAGE_NAME }}:latest \
            .

      - name: Push to OCIR
        run: |
          docker push ${{ env.OCI_REGION }}.ocir.io/${{ env.OCI_OBJECT_STORAGE_NS }}/${{ env.DOCKER_IMAGE_NAME }}:${{ github.sha }}
          docker push ${{ env.OCI_REGION }}.ocir.io/${{ env.OCI_OBJECT_STORAGE_NS }}/${{ env.DOCKER_IMAGE_NAME }}:latest

  deploy:
    name: Deploy to OCI Container Instance
    needs: build-and-push
    runs-on: ubuntu-latest
    steps:
      - name: Set up OCI CLI
        uses: oracle-actions/setup-cli@v1
        with:
          tenancy: ${{ secrets.OCI_TENANCY_OCID }}
          user: ${{ secrets.OCI_USER_OCID }}
          fingerprint: ${{ secrets.OCI_FINGERPRINT }}
          private-key: ${{ secrets.OCI_PRIVATE_KEY }}
          region: ${{ env.OCI_REGION }}

      - name: Retrieve secret from OCI Vault
        id: get-secret
        run: |
          SECRET_B64=$(oci kms management secret get \
            --secret-id "${{ vars.OCI_VAULT_SECRET_ID }}" \
            --query "data.\"secret-content\".content" --raw-output)
          echo "groq_key=$(echo $SECRET_B64 | base64 -d)" >> $GITHUB_OUTPUT

      - name: Update Container Instance
        run: |
          oci compute-container-instance container-instance update \
            --container-instance-id "${{ env.CONTAINER_INSTANCE_ID }}" \
            --containers "[{
              \"displayName\":\"aprendeya-agent-container\",
              \"imageUrl\":\"${{ env.OCI_REGION }}.ocir.io/${{ env.OCI_OBJECT_STORAGE_NS }}/${{ env.DOCKER_IMAGE_NAME }}:${{ github.sha }}\",
              \"environmentVariables\":{
                \"GROQ_API_KEY\":\"${{ steps.get-secret.outputs.groq_key }}\"
              }
            }]" \
            --force

      - name: Wait for health check
        run: |
          PUBLIC_IP=$(oci compute-container-instance container-instance get \
            --container-instance-id "${{ env.CONTAINER_INSTANCE_ID }}" \
            --query "data.\"vnics\"[0].\"public-ip\"" --raw-output)
          echo "Waiting for $PUBLIC_IP:8000/health..."
          for i in $(seq 1 30); do
            STATUS=$(curl -s -o /dev/null -w "%{http_code}" http://$PUBLIC_IP:8000/health 2>/dev/null || echo "000")
            if [ "$STATUS" = "200" ]; then
              echo "Deploy successful!"
              echo "API: http://$PUBLIC_IP:8000/"
              echo "Health: http://$PUBLIC_IP:8000/health"
              exit 0
            fi
            sleep 5
          done
          echo "Health check failed after 150s"
          exit 1
